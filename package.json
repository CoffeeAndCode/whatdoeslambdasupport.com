{
  "name": "whatdoeslambdasupport.com",
  "version": "2.0.0",
  "description": "An autogenerated static website to show what JavaScript functionality is supported out of the box with AWS Lambda with node.js'",
  "main": "app/index.js",
  "scripts": {
    "deploy": "yarn deploy:importTestFiles && yarn deploy:installAppDependencies && yarn deploy:processTemplate && yarn deploy:s3 && yarn deploy:package && yarn deploy:deploy",
    "deploy:deploy": "aws cloudformation deploy --template-file template.deployed.yml --stack-name wdls --capabilities CAPABILITY_IAM",
    "deploy:importTestFiles": "node bin/importTestFiles.js",
    "deploy:installAppDependencies": "cd app && yarn install",
    "deploy:package": "aws cloudformation package --template-file template.processed.yml --output-template-file template.deployed.yml --s3-bucket deploy.whatdoeslambdasupport.com",
    "deploy:processTemplate": "node bin/processTemplate.js",
    "deploy:s3": "aws s3 mb s3://deploy.whatdoeslambdasupport.com --region us-east-1",
    "lint": "eslint --ignore-pattern 'app/node_modules' --ignore-pattern 'app/tests' .",
    "test": "rm -rf dist/ && yarn deploy:importTestFiles && yarn deploy:installAppDependencies && node index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/CoffeeAndCode/whatdoeslambdasupport.com.git"
  },
  "keywords": [
    "aws",
    "lambda",
    "node.js",
    "javascript",
    "support"
  ],
  "author": "Coffee and Code <info@coffeeandcode.com>",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/CoffeeAndCode/whatdoeslambdasupport.com/issues"
  },
  "homepage": "http://whatdoeslambdasupport.com",
  "dependencies": {},
  "devDependencies": {
    "aws-sdk": "^2.32.0",
    "dotenv": "^4.0.0",
    "eslint": "^3.18.0",
    "js-yaml": "^3.8.2",
    "request": "^2.81.0"
  }
}
